{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"'); if echo \"$cmd\" | grep -q 'git commit'; then if ! bash scripts/run-tests.sh 2>&1; then printf '{\"continue\":false,\"stopReason\":\"QC tests failed — commit blocked. Run npm test to see failures.\"}'; exit 1; fi; fi",
            "timeout": 120,
            "statusMessage": "Running QC tests before commit..."
          }
        ]
      }
    ]
  }
}
